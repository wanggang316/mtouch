# Generated

Generated artifacts live here. **Do not hand-edit anything in this directory** —
automation overwrites it.

Nothing is generated into this tree yet. Candidates if the need arises: a rendered
CLI reference from `--help`, or the MCP tool schema dumped from `tools/list`.
Both have a live source of truth today (the binary itself, checked by
`ci/smoke.sh`), so generating a second copy would only add drift.
